[project]
name = "math-exercise-sheets"
version = "0.1.0"
description = "Web app to generate print-ready A4 math worksheets (.docx) for Grade 2–3 students."
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "python-docx>=1.1.2",
    "pydantic>=2.7",
]

[tool.uv]
package = false
